#!/usr/bin/env bash
# Example helper script. Stdlib-only, no external deps, so it travels cleanly
# to every agent. Replace with whatever your skill actually needs.
set -euo pipefail
echo "Hello from example-skill. Args: $*"
